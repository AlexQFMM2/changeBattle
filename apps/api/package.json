{
  "name": "@changebattle-v2/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsc -p tsconfig.json && node dist/server.js",
    "test:boss-team-generator": "tsc -p tsconfig.test.json && node dist-test/boss-team-generator-smoke.js",
    "test:battle-preference": "tsc -p tsconfig.test.json && node dist-test/battle-preference-smoke.js",
    "test:formal-game": "pnpm --filter @changebattle-v2/core build && pnpm --filter @changebattle-v2/showdown-battle-core build && tsc -p tsconfig.test.json && node dist-test/formal-game-smoke.js",
    "test:identity-sync": "tsc -p tsconfig.test.json && node dist-test/identity-sync-smoke.js",
    "test:item-effects": "pnpm --filter @changebattle-v2/core build && tsc -p tsconfig.test.json && node dist-test/item-effects-smoke.js",
    "test:profile-assets": "pnpm --filter @changebattle-v2/core build && tsc -p tsconfig.test.json && node dist-test/profile-assets-smoke.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@changebattle-v2/core": "workspace:*",
    "@changebattle-v2/showdown-battle-core": "workspace:*",
    "@changebattle-v2/showdown-dex-core": "workspace:*",
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0"
  }
}
