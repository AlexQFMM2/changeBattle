# Showdown Battle Core

Node-side Battle V4 adapter for Pokemon Showdown `BattleStream`.

The files under `vendor/showdown` are copied from Pokemon Showdown dist output
and keep their upstream MIT license comments. They are used only by the local
Node battle service; Web/Desktop renderers call this package through HTTP.
